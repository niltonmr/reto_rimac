const Cliapi = require('../api/cliapi')
const Swapi = require('../api/swapi')


test('Responses is an object', () => {
    expect(typeof Cliapi).toBe('object');
});

test('Responses is an object', () => {
    expect(typeof Swapi).toBe('object');
});
